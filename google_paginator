javascript:function extractGoogleSearchBaseUrl(fullUrl) {const urlObj = new URL(fullUrl);const searchParams = urlObj.searchParams;const excludeParams = ['sca_esv', 'rlz', 'ei', 'ved', 'uact', 'oq'];excludeParams.forEach(param => searchParams.delete(param));return `${urlObj.origin}${urlObj.pathname}?${searchParams}`;}function openInNewTabWithInterval(baseUrl, interval) {const startValues = [100, 200, 300, 400];startValues.forEach((start, index) => {setTimeout(() => {window.open(`${baseUrl}&start=${start}`, '_blank');}, index * interval);});}const fullUrl = window.location.href;const baseUrl = extractGoogleSearchBaseUrl(fullUrl);openInNewTabWithInterval(baseUrl, 2000);
